server:
  port: 8080
  servlet:
    context-path: /x-user
spring:
  datasource:
    url:
    username: root
    password: root
  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.yaml
